runs:
  using: "composite"
  steps: 
    - name: Install depedencies
      working-directory: api-deps
      run: |
        echo Building of node application is starting..
        pip3 install -r requirements.txt
      shell: bash